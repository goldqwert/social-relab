const initialState = {
    friends: [
        { id: 1, friend: 'Sasha' },
        { id: 2, friend: 'Andrew' },
        { id: 3, friend: 'Sveta' },
    ]
}

const sidebarReducer = (state = initialState, action) => {
    return state
}

export default sidebarReducer; 